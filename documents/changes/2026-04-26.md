# Changes Log - 2026-04-26

This note records all work done today in simple language.

## Quick Overview

- No new git commit was created today yet.
- There are active changes in 2 files:
  - `app/api/management/mini-gt-import/route.ts`
  - `app/collection/mini-gt/page.tsx`

## What Changed

### 1) Better image import quality and filtering

In `app/api/management/mini-gt-import/route.ts`:

- The import now accepts more image path types (not just one old path).
- The system now **prioritizes better image sources first**.
- It removes duplicate image links and keeps a clean order.
- It only scans the **main product section** of the page, so unrelated "related products" images are avoided.
- Result: image import is more accurate and gives better quality images.

### 2) Mini GT collection page got major usability upgrades

In `app/collection/mini-gt/page.tsx`:

- Added better data typing/structure to make data handling safer.
- Increased fallback local image slots (from 4 to 12).
- Added infinite scroll style loading ("load more while scrolling") for smoother browsing.
- Added image cache-busting support so refreshed images can show newer content.
- Added touch swipe support in the image carousel (mobile-friendly).
- Added a new **Import Image** flow in UI:
  - import modal
  - drag-able modal window
  - series + product URL input
  - actions: "Import + Done" and "Import + Add New"
- Added helper actions like refresh image preview button.
- Improved visual loading placeholders and "showing X/Y" status indicator.
- Cleaned up some old overlay text on carousel images.

## User Impact

- Easier and faster to browse large Mini GT collections.
- Better image reliability and quality.
- Faster manual image import workflow directly from the collection screen.
- Better mobile interaction when viewing images.

## Notes for Next Time

Use this folder for daily notes:

- `documents/changes/`

Suggested naming pattern:

- `YYYY-MM-DD.md`
