# Changes Log - 2026-05-18

This note records all work done today in simple language.

---

## Quick Overview

- Unified app text and navigation styling with icy blue main text (`#bfe9ff`)
- Fixed catalog sidebar (logout), scrolling, and infinite-load UX
- Improved dashboard progress bars, AG Grid list text, and custom slim scrollbars

---

## What Changed

### 1) Theme and main text color (`#bfe9ff`)

- Added a reliable `text-title-highlight` CSS utility in `app/globals.css`
- Standardized main text on sidebar, category brand pills, catalog header, and dashboard headings/stat values
- Sidebar “Diecast Tracker” title size matched to menu items (`text-sm`)
- Dashboard “Spending by Brand” progress tracks now have visible borders; list scrollbars use the dark theme

**Files:** `app/globals.css`, `lib/theme/diecast-theme.ts`, `components/sidebar/sidebar.tsx`, `components/navigation/app-nav-link.tsx`, `components/navigation/app-nav-sheet.tsx`, `app/page.tsx`, `app/catalog/components/CatalogClient.tsx`, `app/catalog/components/BrandTabs.tsx` (via `tw.pillInactive`)

### 2) Shared navigation — logout on catalog

- Extracted `AppNavMenu` and `AppNavFooter` (logout + user email) for reuse
- Desktop sidebar and mobile nav sheet now share the same menu and logout
- Catalog pages include the desktop sidebar in `admin-layout.tsx` (previously bypassed layout with no logout)

**Files:** `components/navigation/app-nav-menu.tsx`, `components/navigation/app-nav-footer.tsx`, `components/sidebar/sidebar.tsx`, `components/navigation/app-nav-sheet.tsx`, `components/layout/admin-layout.tsx`

### 3) Catalog list scroll and infinite load

- Fixed catalog grid not scrolling after sidebar layout change (`min-h-0`, `overflow-y-auto` on scroll container)
- Infinite scroll still loads 20 cards per batch via `IntersectionObserver` on the list `<main>`
- Custom slim scrollbar (4px): card track, primary thumb, border/hover from app palette
- “Loading more…” indicator uses small primary spinner + `#bfe9ff` text

**Files:** `app/catalog/components/CatalogClient.tsx`, `app/globals.css` (`.scrollbar-catalog`)

### 4) AG Grid list pages (purchase, collection, management)

- List/grid cell and header text set to `#bfe9ff` via `.ag-grid-main-text` overrides
- Page headings updated on purchase, collection, and management routes

**Files:** `app/globals.css`, `components/ag-grid/ag-grid-panel.tsx`, `app/purchase/list/page.tsx`, `app/collection/page.tsx`, `app/management/brands/page.tsx`, `app/management/shops/page.tsx`, `app/management/image-import/page.tsx`, `app/purchase/add/page.tsx`

### 5) Theme tokens added

- `tw.pageHeading`, `tw.sectionHeading`, `tw.cardHeading`, `tw.scrollbarCatalog`, `tw.scrollbarDark` (refined)

---

## User Impact

- The app looks more consistent: main labels and list text use the same icy blue across dashboard, catalog, sidebar, and data tables.
- Catalog users can log out from desktop sidebar or mobile menu, scroll the model grid smoothly, and see a thinner scrollbar that matches the dark theme.
- Admin list pages (purchases, collection, brands, shops) are easier to read with matching text color and progress/scroll styling on the dashboard.

---

## Notes for Next Time

- Hard refresh (Cmd+Shift+R) if colors or scrollbars do not update after deploy.
- Active nav items and selected brand pills still use teal/white for clear “selected” state.
